export default function Dashboard() {
  return (
    <main style={{padding:24}}>
      <h2>🏁 Demo Clinic — Dashboard</h2>
      <p>If you see this, routing works. Next we’ll add real login.</p>
      <a className="btn" href="https://process.env.NEXT_PUBLIC_BASE_URL" target="_blank" rel="noreferrer">Open Inbox</a>
    </main>
  );
}
